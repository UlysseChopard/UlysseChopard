mixin item(data)
    .item
        each value, key in data
            span(class=key)= value
            // - trailing space
            | 

mixin section(data)
    .section
        each value, _key in data
            +item(value)