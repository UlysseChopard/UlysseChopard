doctype html
include mixins.pug
html(lang="en")
    head
        +head(name, description, title)
        style
            include ../css/elements.css
            include ../css/classes.css
            include ../css/ids.css
    body 
        header
        
        main
            block main
                each value, key in cursus 
                    h2= key
                    +section(value)